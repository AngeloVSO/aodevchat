import styled from "styled-components";

const MainArea = styled.main`
    max-width: 250px;
    margin: 0 auto;
    text-align: center;

    form {
        display: grid;
        gap: 8px;
    }
`

export default MainArea